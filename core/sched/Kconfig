menu "Scheduler"

choice
    prompt "Process scheduler"
    default SCHED_PREEMPTIVE_RR

config SCHED_COOPERATIVE_FIFO
    bool "Cooperative FIFO"

config SCHED_PREEMPTIVE_RR
    bool "Preemptive Round Robin"

endchoice

endmenu
